# =============================================================================
# merchant-app.env.docker — runtime env for the Next.js merchant app container
# Copy this file, fill in real values, and pass via --env-file to docker run
# or reference it in docker-compose env_file.
# NEVER commit filled-in secrets to git.
# =============================================================================

# Server
PORT=5001
HOSTNAME=0.0.0.0
NODE_ENV=production
API_PROXY_TARGET=http://crypto-payment-be:3000